{
  "name": "homebridge-twine-temp",
  "version": "0.0.1",
  "description": "Temp sensor from Twine via http",
  "dependencies": {
    "node-fetch": "^1.7.3"
  },
  "keywords": [
    "homebridge-plugin",
    "http",
    "twine",
    "temperature sensor"
  ],
  "engines": {
    "node": ">=0.12.0",
    "homebridge": ">=0.2.0"
  }
}
